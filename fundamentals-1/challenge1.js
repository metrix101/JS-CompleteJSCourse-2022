// let johnWeights, johnHeight;
// let markWeights, markHeight;

// markWeights = 78;
// markHeight = 1.69;

// johnWeights = 92;
// johnHeight = 1.95

// const johnBmi = johnWeights / (johnHeight ** 2);
// const markBmi = markWeights / (markHeight ** 2);

// console.log("John Body Mass Index is", johnBmi, "And Mark BMI is", markBmi);

// const markHigherBMI = markBmi > johnBmi;
// console.log(markHigherBMI);


// markWeights = 95;
// markHeight = 1.88;

// johnHeight = 1.76;
// johnWeights = 85;

// const johnBMI = johnWeights / (johnHeight ** 2);
// const markBMI = markWeights / (markHeight ** 2);

// console.log(johnBMI, markBMI)
// const markHigherBMi = markBMI > johnBMI
// console.log(markHigherBMi)

// if (johnBmi > markBmi) {
//     console.log(`John's BMI is ${johnBmi} and,
// Hence, Higher than that of Mark`)
// } else {
//     console.log(`Mark's BMI is ${markBmi}
//     and hence is higher than that of John`)
// }

